@startuml
start

:expression = self._parse_implication();

while (self._match(TokenType.EQUIV)?) is (да)
  :operator = self._previous();
  :right = self._parse_implication();
  :expression = BinaryExpression(operator.value, expression, right);
endwhile (нет)

:return expression;
stop
@enduml
